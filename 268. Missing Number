class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int current_sum = 0;
        int n = nums.size();
        int ideal_sum = (n*(n+1))/2;
        for (int i = 0; i < n; i++) {
            current_sum += nums[i];
        }
        return ideal_sum - current_sum;
    }
};
